import type { Metadata } from "next"

// Metadata yang dipindahkan dari layout.tsx
export const metadata: Metadata = {
  title: "Insighthink",
  description: "A learning platform for self taught learners",
}
